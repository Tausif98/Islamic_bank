
# Web Framework
fastapi[all]==0.128.0          # latest stable
email-validator==2.2.0

# ASGI Server (included in fastapi[all] but pin it anyway)
uvicorn[standard]==0.38.0
gunicorn==24.0.0         # use both for best practice in production

motor==3.7.1
pymongo==4.15.0  # alternative for motor as it is depricated by May 2025
beanie==2.0.0

# Kafka async
aiokafka==0.12.0

# Redis
redis==6.4.0


# Background tasks
# celery==5.6.0
celery[redis]==5.6.0

# Settings management
pydantic-settings==2.10.0

# JWT Authentication
Authlib==1.6.5   # pythonjson
pwdlib==0.3.0   # passlib 
bcrypt==5.0.0


# File upload support (included in fastapi[all])
python-multipart==0.0.20


# Testing
pytest==9.0.0
pytest-asyncio==1.3.0
pytest-env==1.2.0
httpx==0.28.1    # an alternative to aiohttp as it can help in both sync and async but still for async only program/work aiohttp is better choice

# Utilities
pendulum==3.1.0    # python-dateutil